# TensorBoard logger configuration
# The plain PyTorch training script uses torch.utils.tensorboard.SummaryWriter directly
tensorboard:
  save_dir: "${paths.output_dir}/tensorboard/"
  log_graph: false
