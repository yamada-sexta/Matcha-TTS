# Callbacks configuration (not used in plain PyTorch training loop)
# The training script handles checkpointing and progress display internally
model_checkpoint:
  save_top_k: 1
  monitor: "loss/val"
  mode: "min"
  save_last: true
